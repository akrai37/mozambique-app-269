name: mozambique_app
description: "This offline-first mobile app teaches Portuguese to non-literate mothers in rural Mozambique through visual and auditory learning, accommodating users with no reading or technology experience"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.2.3+7

environment:
  sdk: ^3.6.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  audioplayers: ^6.4.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2  # Needed for storage location
  firebase_core: ^4.4.0
  cloud_firestore: ^6.1.2
  http: ^1.3.0
  connectivity_plus: ^7.0.0
  firebase_storage: ^13.0.6
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  
  hive_generator: ^2.0.0  # For code generation
  build_runner: ^2.4.6    # Runs code generation

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    # Content manifests, loaded by LocalContentService.
    # Listed individually rather than as a directory so the 55 MB
    # vocab_words.json (superseded by vocab_words.slim.json) and the demo
    # fixtures alongside it stay out of the bundle.
    - assets/json/home_cards.json
    - assets/json/vocab_words.slim.json
    - assets/json/learn_convo.json
    - assets/json/practice_convo.json
    - assets/json/practice_quiz.json

    # UI chrome
    - assets/images/
    - assets/images/Prac-Body/
    - assets/images/Prac-Face/
    - assets/images/Practice/
    - assets/images/logos/

    # Content imagery
    - assets/images/greetings_requests/
    - assets/images/learn/
    - assets/images/practice_convo/
    - assets/images/practice_quiz/body/
    - assets/images/practice_quiz/colors/
    - assets/images/practice_quiz/face/
    - assets/images/vocab_words/alphabet/
    - assets/images/vocab_words/animals/
    - assets/images/vocab_words/body/
    - assets/images/vocab_words/colors/
    - assets/images/vocab_words/common_actions/
    - assets/images/vocab_words/everyday_activities/
    - assets/images/vocab_words/face/
    - assets/images/vocab_words/fruits/
    - assets/images/vocab_words/household_items/
    - assets/images/vocab_words/kitchen_items/
    - assets/images/vocab_words/numbers/
    - assets/images/vocab_words/professions/
    - assets/images/vocab_words/vegetables/

    # Content audio
    - assets/audio/quiz_prompts/
    - assets/audio/learn_convo/greetings/
    - assets/audio/learn_convo/requests/
    - assets/audio/practice_convo/greetings/
    - assets/audio/practice_convo/personal_interactions/
    - assets/audio/practice_convo/requests/
    - assets/audio/practice_quiz/body/
    - assets/audio/practice_quiz/colors/
    - assets/audio/practice_quiz/face/
    - assets/audio/vocab_words/alphabet/
    - assets/audio/vocab_words/animals/
    - assets/audio/vocab_words/body/
    - assets/audio/vocab_words/colors/
    - assets/audio/vocab_words/common_actions/
    - assets/audio/vocab_words/everyday_activities/
    - assets/audio/vocab_words/face/
    - assets/audio/vocab_words/fruits/
    - assets/audio/vocab_words/household_items/
    - assets/audio/vocab_words/kitchen_items/
    - assets/audio/vocab_words/numbers/
    - assets/audio/vocab_words/professions/
    - assets/audio/vocab_words/vegetables/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: AvenirLTStd
      fonts:
        - asset: fonts/AvenirLTStd-Roman.otf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

